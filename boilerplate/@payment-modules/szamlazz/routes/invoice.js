/*ßoilerplate */

// @DOC by default invoices are saved to a default path that can be overridden in a config - or leave to false;

// ß.CWD + "/invoice" by default set in init.

// if (ß.szamlazz_config.save_path) ß.static(ß.szamlazz_config.save_path);
