/*jshint esnext: true */

if (process.config.variables.node_path) console.log('- node_path ' + process.config.variables.node_path);
if (process.env.NODE_PATH) console.log("- loading modules from: NODE_PATH=" + process.env.NODE_PATH);

// @DOC Instead of fs = require('fs') you can use ß.fs
// @DOC By default, ß.fs refers to the the fs-extra package, so that you can use mkdirp, and readJson functions. 

console.log("- ß.fs is fs-extra from " + require.resolve('fs-extra'));
ß.fs = require('fs-extra');

// additional global modules can be defined....
// @DOC you may specify your superglobal node_modules, like underscore, or async.js in the global-node_modules.js file in your project-root.
if (ß.fs.existsSync(ß.CWD + '/node_modules.js')) require(ß.CWD + '/node_modules.js');