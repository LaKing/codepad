if (!ß.PROJECTDIR) ß.PROJECTDIR = '/srv/codepad-project';
if (!ß.TRASHDIR) ß.TRASHDIR = ß.CWD + '/.TRASH';

// in case we dont use the theme module
if (!ß.THEME) ß.THEME = 'cobalt';
if (!ß.THEMES) ß.THEMES = [];